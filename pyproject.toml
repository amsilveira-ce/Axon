[project]
name = "axon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28.1",
    "pydantic>=2.13.3",
    "typer>=0.25.1",
]

[project.scripts]
axon = "axon.cli.main:app"
